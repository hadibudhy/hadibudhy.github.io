# UI work requirements

Own only src/app/page.tsx, src/app/about/page.tsx, src/app/layout.tsx, src/app/projects/page.tsx,
src/app/projects/[slug]/page.tsx, src/lib/profile.ts, src/lib/mdx.ts, src/components/ui/ProjectCard.tsx,
src/components/layout/Footer.tsx, src/app/sitemap.ts. Do not edit project Markdown, analytics/, .github/, package files or images.
Work in D:/projects/hadibudhy.github.io/.worktrees/ae-product, not original checkout.
Do not delegate, commit or push. Other work is in progress; preserve it.

User positioning: Analytics Engineer first, Product Analyst second. Keep visual design and photo.
Heading: Analytics Engineer & Product Analyst. Body: I build reliable analytics models and use product data to understand
customer behavior, define consistent metrics, and help teams decide what to improve next.
Group exactly one card per case into Analytics Engineering, Product Analytics, Experimentation & Growth.
Homepage shows featured cases only, maximum6. Library same categories; no duplicate cards in secondary tracks.
Remove generic DA/AppliedAI positioning in metadata/CTA/footer and capability pitches.
Keep actual past employment role titles, dates, privacy boundaries, education, links.
About connects instrumentation, modeling, metrics, behavior, decisions, measurement in natural B2 copy.
No interview language, fabricated results, 10YOE claims, or claimed production operation of independent projects.

ProjectMeta additions: caseId:string; primaryTrack: 'analytics-engineering'|'product-analytics'|'experimentation-growth';
secondaryTracks: same[]; displayOrder:number; evidenceManifest:string (public /data/evidence/{caseId}.json).
Explicit published:true is required, missing/false hides page. Validate identity, category, order, evidence manifest path
for public pages; keep kind:'flagship'|'completed'|'methods', evidenceVisuals and >=3 evidence visual gate.
Reject duplicate caseIds in getAllProjects. Sort displayOrder asc then slug stable. Hidden pages need no new fields.
Evidence manifest JSON will be generated by main agent: schemaVersion:1, caseId, scope:'full-data', status:'verified',
sources[], metrics{}, checks{}, models[], artifacts[] (path,sha256). Loader only needs check file exists,
matching caseId/status/scope; full integrity is handled by the Python publication check.
getAllProjects is shared by homepage/library/sitemap/static params. No mock evidence files.
Do not panic when build fails until main agent creates final manifests and six content pages.

Card: clear question + contribution/evidence signal and primary track, short tags; link to case page.
Technical appendix stays readable with tables scrolling locally, details/summary keyboard native if used.
Read local Next docs before code. Verify lint and typecheck; report exact changed files and remaining integration checks
in docs/ae-product-ui-report.md (only extra owned file).
